find * -prune -type f -exec \sh -c 'pdftoppm -png "$0" "${0%.*}" && mv *.png ../../Images/"${0%.*}"' {} \;
for each pdf file in a folder do the following
    convert the pdf to an image and call it it's orignal name minus the ".pdf"
    move the images from the pdf file to a folder named after the pdf file
    

find * -type f -name "*.pptx" | while read line; do echo $line; done


 find * -type f -name "*.pptx" | while read line; do libreoffice --convert-to pdf --outdir "$temp" "$line"; done
 for each pptx file in a folder or subfolder do the following
     convert the pptx to an pdf and output it to the value of variable temp

Search and replace within files using sed
find /path/to/files -type f -exec sed -i 's/oldstring/new string/g' {} \;

find . -name "*.pptx" | while read line; do mv "$line" $(echo "$line"| sed -e 's/ //g') ; done
for each .pptx file rename the file without any whitespace

for f in *\ *; do mv "$f" "${f// /_}"; done
remove white space from file names
stow -nvS dotconfig -t ~/.config/
stow all the files in "dotconfig" to ~/.config, i.e create a symbolink from the files in "dotconfig" directory to apear in ~/.config/

find . -type f | fzf --multi  | sed 's/.*/"&"/g' | xargs okular
open files with okular matched with fzf
find all the files in a directory and subdirectories
pipe it to fzf (fuzzy finder) in a multi selection mode
choices from fzf get piped to sed which replaces any-character many-times (.*) with the portion of the matched pattern (&) surrounded by quotes
note sed 's/./"&"/g' will put quotes around every character (foo becomes "f""o""o")

ssh pv@pvlabserver.nmmu.ac.za -X sudo -S -u pv nm-connection-editor 
open a graphical program from a remote machine on the local machine with sudo privileges

terminal commands:
Alt+f move forward a word
Alt+b move back a word
Ctrl+a goto start of line
Ctrl+e goto to end of line
Ctrl+u delete before cursor to start of line
Ctrl+k delete after cursor to end of line
Ctrl+w delete word before cursor 
Alt+d delete word after cursor
Alt+t moves word behind previous word

for more https://gist.github.com/tuxfight3r/60051ac67c5f0445efee


git tag | grep "[0-9]" | sort --reverse | head -n 1
pipes get tags into grep which filters for digits and then pipes that into sort which gets highest number then into head which gets first result
stow: 


git rebase after commit:
git rebase -i <After this commit Id>
Then replace "pick" on second and subsequent commits with "squash"

git modify commit:
git rebase -i <commit ID>~ 
Then replace "pick" with "edit"
the "~" is necessary as you need to reapply commits on top of the previous commit

stow -nvS .config/ -t ~/.config
will create siymbolic links from all files in .config to ~/.config
